<div class="jumbotron center">
	<h1>Welcome to Goonterest!</h1>
	<p>The Goon Pinterest.</p>
	<% if !user_signed_in? %>
		<p><%= link_to "Log In", new_user_session_path, class: "btn btn-default btn-large" %> <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-large" %></p>
	<% end %>
	<% if user_signed_in? %>
		<p><%= link_to "Log Out", destroy_user_session_path, :method => :delete, class: "btn btn-default btn-large" %></p>
	<% end %>

</div>